<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BOLDApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="Theme.BOLDApp" parent="Base.Theme.BOLDApp" />

    <style name="DialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="colorAccent">@color/orange</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/light_black</item>
        <item name="android:windowBackground">@color/light_black</item>
        <item name="android:color">@color/light_orange</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="textColorAlertDialogListItem">@color/orange</item>
        <item name="materialAlertDialogTheme">@style/CustomMaterialAlertDialog</item>
    </style>
    <style name="CustomMaterialAlertDialog" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:textColor">@color/orange</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>